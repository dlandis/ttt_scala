Tic Tac Toe in Scala

1. Install Simple Build Tool with homebrew

terminal> brew install sbt

2. Start sbt in project directory

terminal> sbt

This should download the required scala version.

3. To get managed dependencies, inside sbt type:

update

3. To run tests, inside sbt type:

test

4. To play tic tac toe, inside sbt type:

run
